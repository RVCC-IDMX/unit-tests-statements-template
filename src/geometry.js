/* geometry.js */

/**
 * return the perimeter of the rectangle with
 * sides width and height
 * to 2 decimal places
 *
 * @param {number} width
 * @param {number} height
 * @returns {number}
 *
 * Hint: you can use toFixed()
 */

function rectanglePerimeter(width, height) {
  // write your code here
}

/**
 * return the area of the rectangle with
 * sides width and height
 * to 2 decimal places
 *
 * @param {number} width
 * @param {number} height
 * @returns {number}
 *
 * Hint: you can use toFixed()
 */
function rectangleArea(width, height) {
  // write your code here
}

/**
 * return the circumference of a circle with a known radius
 * to 2 decimal places
 * @param {number} radius
 * @returns {number}
 *
 * The math equation is c = 2 * pi * radius
 *
 * Hint: use toFixed()
 */
function circleCircumference(radius) {
  // write your code here
}

/**
 * return the area of a circle with a known radius
 * to 2 decimal places
 *
 * @param {number} radius
 * @returns {number}
 */
function circleArea(radius) {
  // write your code here
}

/**
 *
 * @param {number} sideA
 * @param {number} sideB
 * @returns {number}
 */
function pythagorean(sideA, sideB) {
  // write your code here
}

module.exports = {
  rectanglePerimeter,
  areaRectangle,
  circumferenceCircle,
  areaCircle,
  pythagorean,
};
